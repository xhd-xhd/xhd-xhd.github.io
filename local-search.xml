<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Kbuild 配置系统逻辑梳理——Kconfig、defconfig 与 menuconfig 的背后</title>
    <link href="/2026/07/19/kbuild-config-system/"/>
    <url>/2026/07/19/kbuild-config-system/</url>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>每次 <code>make menuconfig</code> 弹出那个蓝底菜单界面的时候，我从没认真想过背后到底发生了什么。Kconfig、defconfig、.config、auto.conf……这些文件到底是干嘛的，谁生成的，谁消费的？配置选项是怎么从菜单点击变成 C 代码里的 <code>#define</code> 的？</p><p>今晚把这条路走了一遍，总算理清了。这篇文章记录 Kbuild 配置系统的核心文件、工具和数据流转关系，不只是 Linux 内核，U-Boot 等同样采用 Kbuild 的项目都适用。</p><hr><h2 id="1-核心文件与工具定义"><a href="#1-核心文件与工具定义" class="headerlink" title="1. 核心文件与工具定义"></a>1. 核心文件与工具定义</h2><table><thead><tr><th>名称</th><th>类型</th><th>角色</th><th>生成方式</th></tr></thead><tbody><tr><td><strong>Kconfig</strong></td><td>文本文件</td><td><strong>配置项定义</strong>。定义所有可用的 <code>CONFIG_XXX</code> 选项、类型、依赖关系及帮助信息。</td><td>开发者手写</td></tr><tr><td><strong>defconfig</strong></td><td>文本文件</td><td><strong>默认配置模板</strong>。仅包含与 Kconfig 默认值不同的配置项（差异集）。</td><td>开发者手写 &#x2F; <code>make savedefconfig</code> 生成</td></tr><tr><td><strong>.config</strong></td><td>文本文件</td><td><strong>最终用户配置</strong>。包含所有配置项的完整状态（<code>y</code> &#x2F; <code>m</code> &#x2F; <code>n</code> &#x2F; <code># is not set</code>）。</td><td><code>conf</code> 程序根据 <code>defconfig</code> + <code>Kconfig</code> 生成</td></tr><tr><td><strong>autoconf.h</strong></td><td>C 头文件</td><td><strong>C 语言宏定义</strong>。将 <code>.config</code> 转换为 <code>#define CONFIG_XXX 1</code>，供源码条件编译使用。</td><td><code>conf</code> 程序根据 <code>.config</code> 生成</td></tr><tr><td><strong>auto.conf</strong></td><td>Makefile 片段</td><td><strong>构建变量赋值</strong>。将 <code>.config</code> 转换为 <code>CONFIG_XXX=y</code>，供 Makefile 读取以决定编译目标。</td><td><code>conf</code> 程序根据 <code>.config</code> 生成</td></tr><tr><td><strong>conf</strong></td><td>C 可执行程序</td><td><strong>配置解析器</strong>。负责解析 <code>Kconfig</code> &#x2F; <code>defconfig</code> &#x2F; <code>.config</code>，并生成上述各种格式的输出。</td><td>由构建系统编译 <code>scripts/kconfig/conf.c</code></td></tr><tr><td><strong>mconf</strong></td><td>C 可执行程序</td><td><strong>图形化前端</strong>。基于 ncurses 库，提供交互式界面修改 <code>.config</code>。</td><td>由构建系统编译 <code>scripts/kconfig/mconf.c</code></td></tr></tbody></table><hr><h2 id="2-Makefile-规则与调用逻辑"><a href="#2-Makefile-规则与调用逻辑" class="headerlink" title="2. Makefile 规则与调用逻辑"></a>2. Makefile 规则与调用逻辑</h2><p>顶层 <code>Makefile</code> 中定义了以下核心规则，用于调度配置过程。</p><h3 id="2-1-make-xxx-defconfig"><a href="#2-1-make-xxx-defconfig" class="headerlink" title="2.1 make xxx_defconfig"></a>2.1 <code>make xxx_defconfig</code></h3><ul><li><strong>依赖</strong>：<code>scripts/kconfig/conf</code>（确保解析器已编译）</li><li><strong>执行命令</strong>：<code>scripts/kconfig/conf --defconfig=arch/xxx/configs/xxx_defconfig Kconfig</code></li></ul><p><strong>处理逻辑</strong>：</p><ol><li><code>conf</code> 程序读取 <code>Kconfig</code> 获取所有选项定义。</li><li><code>conf</code> 程序读取 <code>xxx_defconfig</code> 获取用户预设的差异配置。</li><li><code>conf</code> 程序合并两者，生成根目录下的 <code>.config</code> 文件。</li></ol><h3 id="2-2-make-menuconfig"><a href="#2-2-make-menuconfig" class="headerlink" title="2.2 make menuconfig"></a>2.2 <code>make menuconfig</code></h3><ul><li><strong>依赖</strong>：<code>scripts/kconfig/mconf</code>（确保图形前端已编译）</li><li><strong>执行命令</strong>：<code>scripts/kconfig/mconf Kconfig</code></li></ul><p><strong>处理逻辑</strong>：</p><ol><li><code>mconf</code> 程序读取 <code>Kconfig</code> 渲染菜单结构。</li><li>若存在 <code>.config</code>，则读取其内容初始化菜单选中状态；若不存在，则使用默认值初始化。</li><li>用户在界面修改后，<code>mconf</code> 将结果保存回 <code>.config</code>。</li></ol><h3 id="2-3-make（编译阶段）"><a href="#2-3-make（编译阶段）" class="headerlink" title="2.3 make（编译阶段）"></a>2.3 <code>make</code>（编译阶段）</h3><ul><li><strong>依赖</strong>：<code>.config</code></li><li><strong>自动触发</strong>：在编译任何目标前，Makefile 会检查 <code>.config</code> 是否比 <code>autoconf.h</code> 新。若是，则自动调用 <code>conf</code> 程序进行转换。</li><li><strong>执行命令</strong>：<code>scripts/kconfig/conf --silentoldconfig Kconfig</code></li></ul><p><strong>处理逻辑</strong>：</p><ol><li><code>conf</code> 读取 <code>.config</code>。</li><li>生成 <code>include/generated/autoconf.h</code>（供 C 代码 <code>#include</code>）。</li><li>生成 <code>include/config/auto.conf</code>（供 Makefile <code>include</code>）。</li></ol><hr><h2 id="3-数据流转与依赖关系"><a href="#3-数据流转与依赖关系" class="headerlink" title="3. 数据流转与依赖关系"></a>3. 数据流转与依赖关系</h2><pre><code class=" mermaid">graph TD    A[Kconfig&lt;br/&gt;开发者手写] --&gt; B(conf 程序)    C[defconfig&lt;br/&gt;开发者手写] --&gt; B    B --&gt;|生成| D[.config&lt;br/&gt;完整配置]    D --&gt; E(mconf 程序)    E --&gt;|读取/修改| D    D --&gt;|编译时自动触发| F(conf 程序)    F --&gt;|生成| G[autoconf.h&lt;br/&gt;C 头文件]    F --&gt;|生成| H[auto.conf&lt;br/&gt;Makefile 片段]    G --&gt; I[C 源代码&lt;br/&gt;条件编译]    H --&gt; J[Makefile&lt;br/&gt;决定编译目标]</code></pre><hr><h2 id="4-关键逻辑总结"><a href="#4-关键逻辑总结" class="headerlink" title="4. 关键逻辑总结"></a>4. 关键逻辑总结</h2><ol><li><strong>Kconfig 是源头</strong>：它定义了”有什么”，是所有配置的基础。</li><li><strong>defconfig 是起点</strong>：它定义了”默认选什么”，用于快速初始化 <code>.config</code>。</li><li><strong>.config 是核心</strong>：它是用户选择的最终记录，也是后续所有转换的唯一输入源。</li><li><strong>conf 是转换器</strong>：它在不同阶段扮演不同角色：<ul><li><strong>初始化阶段</strong>：<code>Kconfig + defconfig → .config</code></li><li><strong>构建阶段</strong>：<code>.config → autoconf.h + auto.conf</code></li></ul></li><li><strong>Makefile 是调度器</strong>：它不处理配置逻辑，只负责在正确的时间调用 <code>conf</code> 或 <code>mconf</code>，并将生成的 <code>auto.conf</code> 纳入构建环境。</li></ol><hr><h2 id="5-实际应用场景"><a href="#5-实际应用场景" class="headerlink" title="5. 实际应用场景"></a>5. 实际应用场景</h2><table><thead><tr><th>场景</th><th>命令</th><th>说明</th></tr></thead><tbody><tr><td>使用已有默认配置</td><td><code>make xxx_defconfig</code></td><td>从 <code>arch/xxx/configs/</code> 下加载预设配置</td></tr><tr><td>保存当前配置为模板</td><td><code>make savedefconfig</code></td><td>将当前 <code>.config</code> 精简为 defconfig 格式</td></tr><tr><td>图形化修改配置</td><td><code>make menuconfig</code></td><td>基于 ncurses 的交互式配置界面</td></tr><tr><td>仅更新配置（不改动选项）</td><td><code>make olddefconfig</code></td><td>根据 Kconfig 更新 <code>.config</code>，新增选项使用默认值</td></tr><tr><td>列出所有可用 defconfig</td><td><code>make help</code></td><td>查看平台默认配置列表</td></tr></tbody></table><hr><p><em>理解这套机制后，不管是新增一个配置选项、排查某个功能有没有编译进去，还是给新平台写一个 defconfig，脉络都很清楚。</em></p>]]></content>
    
    
    <categories>
      
      <category>构建系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Kconfig</tag>
      
      <tag>Kbuild</tag>
      
      <tag>menuconfig</tag>
      
      <tag>构建系统</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>正点原子 i.MX6ULL 通过 TFTP + NFS 启动全记录</title>
    <link href="/2026/07/12/imx6ull-tftp-nfs-boot/"/>
    <url>/2026/07/12/imx6ull-tftp-nfs-boot/</url>
    
    <content type="html"><![CDATA[<h1 id="🚀-正点原子-i-MX6ULL-通过-TFTP-NFS-启动全记录"><a href="#🚀-正点原子-i-MX6ULL-通过-TFTP-NFS-启动全记录" class="headerlink" title="🚀 正点原子 i.MX6ULL 通过 TFTP + NFS 启动全记录"></a>🚀 正点原子 i.MX6ULL 通过 TFTP + NFS 启动全记录</h1><h2 id="为什么要折腾-NFS-启动？"><a href="#为什么要折腾-NFS-启动？" class="headerlink" title="为什么要折腾 NFS 启动？"></a>为什么要折腾 NFS 启动？</h2><p>正点原子 Alpha 开发板默认从 eMMC 或 SD 卡启动。每次改完驱动、调完内核，都要把镜像烧进 SD 卡、拔下来插板子上、上电……循环往复，一天下来 SD 卡槽都快拔烂了。</p><p><strong>TFTP + NFS 启动</strong> 能彻底解决这个问题：</p><ul><li><strong>内核和 DTB</strong> 通过 TFTP 下载，U-Boot 直接从 PC 拉取</li><li><strong>根文件系统</strong> 通过 NFS 挂载，板子上跑的文件直接存在 PC 硬盘上</li><li><strong>改了驱动</strong>，PC 上编译完，板子重启就能跑——不用烧录、不用拔卡</li></ul><p>本文记录从零折腾这玩意的全过程，踩过的坑都写下来了。</p><h2 id="硬件环境"><a href="#硬件环境" class="headerlink" title="硬件环境"></a>硬件环境</h2><table><thead><tr><th>项目</th><th>详情</th></tr></thead><tbody><tr><td>开发板</td><td>正点原子 I.MX6U ALPHA (eMMC 版)</td></tr><tr><td>芯片</td><td>NXP i.MX6ULL, Cortex-A7, 792MHz</td></tr><tr><td>内存</td><td>512MB DDR3</td></tr><tr><td>屏幕</td><td>7 寸 800×480 RGB LCD</td></tr><tr><td>U-Boot</td><td>NXP 2016.03 (<code>mx6ull_alientek_emmc_defconfig</code>)</td></tr><tr><td>PC</td><td>Arch Linux x86_64</td></tr></tbody></table><p><img src="/img/linux_alpha/board_connection1.jpg" alt="开发板连线全景"><br><img src="/img/linux_alpha/board_connection2.jpg" alt="开发板近景"></p><h2 id="PC-侧环境搭建"><a href="#PC-侧环境搭建" class="headerlink" title="PC 侧环境搭建"></a>PC 侧环境搭建</h2><h3 id="0-串口终端"><a href="#0-串口终端" class="headerlink" title="0. 串口终端"></a>0. 串口终端</h3><p>开发板通过 <strong>USB-TTL 串口模块</strong> 连接到 PC，一根 Micro USB 线插在板子的 <code>USB_TTL</code> 口上，另一头插入 PC 的 USB 口，在 PC 上会识别为一个串口设备（如 <code>/dev/ttyUSB0</code>）。</p><p>这里用 <a href="https://github.com/npat-efault/picocom">picocom</a> 作为串口终端——比 minicom 轻量，不需要配置文件，一行命令搞定：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 安装</span><br><span class="hljs-built_in">sudo</span> pacman -S picocom<br><br><span class="hljs-comment"># 连接开发板 (115200 波特率，8N1)</span><br>picocom -b 115200 /dev/ttyUSB0<br></code></pre></td></tr></table></figure><p>退出按 <code>Ctrl+A</code> 再按 <code>Ctrl+X</code>。</p><blockquote><p>如果提示权限不够，把自己加到 <code>uucp</code> 组：<code>sudo usermod -aG uucp $USER</code>，重新登录后生效。</p><p>串口模块用的是 CH340 芯片，Arch Linux 内核自带驱动，即插即用。</p></blockquote><h3 id="1-TFTP-服务器"><a href="#1-TFTP-服务器" class="headerlink" title="1. TFTP 服务器"></a>1. TFTP 服务器</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 安装</span><br><span class="hljs-built_in">sudo</span> pacman -S tftp-hpa<br><br><span class="hljs-comment"># 配置 /etc/conf.d/tftpd</span><br>TFTPD_ARGS=<span class="hljs-string">&quot;--secure /home/xhd/linux/tftpboot/&quot;</span><br><br><span class="hljs-comment"># 启动</span><br><span class="hljs-built_in">sudo</span> systemctl start tftpd<br></code></pre></td></tr></table></figure><h3 id="2-NFS-服务器"><a href="#2-NFS-服务器" class="headerlink" title="2. NFS 服务器"></a>2. NFS 服务器</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 安装</span><br><span class="hljs-built_in">sudo</span> pacman -S nfs-utils<br><br><span class="hljs-comment"># /etc/exports</span><br>/home/xhd/linux/nfs_rootfs 192.168.1.200/32(rw,<span class="hljs-built_in">sync</span>,no_root_squash,no_subtree_check)<br><br><span class="hljs-comment"># 生效</span><br><span class="hljs-built_in">sudo</span> exportfs -rv<br><span class="hljs-built_in">sudo</span> systemctl start nfs-server<br></code></pre></td></tr></table></figure><h3 id="3-有线网口固定-IP"><a href="#3-有线网口固定-IP" class="headerlink" title="3. 有线网口固定 IP"></a>3. 有线网口固定 IP</h3><p>板子和 PC 通过 USB 以太网卡直连，需要给 PC 侧固定一个同网段的 IP：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 用 NetworkManager 持久化配置</span><br><span class="hljs-built_in">sudo</span> nmcli con add <span class="hljs-built_in">type</span> ethernet \<br>  ifname enp4s0f3u1u5c2 \<br>  con-name alpha-board \<br>  ipv4.addresses 192.168.1.100/24 \<br>  ipv4.method manual<br><br><span class="hljs-built_in">sudo</span> nmcli con up alpha-board<br></code></pre></td></tr></table></figure><blockquote><p><strong>坑</strong>：别用 <code>ip addr add</code>，那是临时的，NetworkManager 一刷新就没，开发过程中网络反复断。</p></blockquote><h2 id="编译内核"><a href="#编译内核" class="headerlink" title="编译内核"></a>编译内核</h2><p>正点原子资料里带了内核源码（<code>linux-imx-4.1.15</code>），但我们重新编译一遍，确保 NFS Root 支持正确配置。</p><h3 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> ~/linux/kernel/linux-imx-4.1.15<br><span class="hljs-built_in">export</span> PATH=/opt/toolchains/gcc-linaro-4.9.4-2017.01-x86_64_arm-linux-gnueabihf/bin:<span class="hljs-variable">$PATH</span><br><br>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- imx_alientek_emmc_defconfig<br></code></pre></td></tr></table></figure><p><strong>必须确认以下选项全部是 <code>=y</code>（内置），不能是 <code>=m</code>（模块）</strong>：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">CONFIG_NFS_FS</span>=y              <span class="hljs-comment"># NFS 客户端</span><br><span class="hljs-attr">CONFIG_ROOT_NFS</span>=y            <span class="hljs-comment"># NFS 作为根文件系统</span><br><span class="hljs-attr">CONFIG_NFS_V3</span>=y              <span class="hljs-comment"># NFSv3（root mount 用 v3 最稳）</span><br><span class="hljs-attr">CONFIG_FEC</span>=y                 <span class="hljs-comment"># FEC 以太网驱动（必须内置！）</span><br><span class="hljs-attr">CONFIG_IP_PNP</span>=y              <span class="hljs-comment"># 内核级 IP 配置</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>坑 1</strong>：<code>CONFIG_FEC=m</code> 是最坑的——模块在挂根文件系统时还没加载，内核找不到网卡，NFS mount 直接跪。</p><p><strong>坑 2</strong>：<code>CONFIG_NFS_V4</code> 虽然也开着，但 NFS root 走 v4 需要额外的用户态服务配合，v3 反而最简单可靠。</p></blockquote><h3 id="编译踩坑"><a href="#编译踩坑" class="headerlink" title="编译踩坑"></a>编译踩坑</h3><p><strong>坑 3：GCC 10+ 编译旧内核 dtc 报错</strong></p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs asciidoc">multiple definition of <span class="hljs-string">`yylloc&#x27;</span><br></code></pre></td></tr></table></figure><p>原因是 GCC 10+ 默认开启 <code>-fno-common</code>，旧内核的 dtc 源码有全局变量重复定义。修复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 编辑 scripts/dtc/Makefile，找到这一行：</span><br>HOSTCFLAGS_DTC := -I$(src) -I$(src)/libfdt<br><br><span class="hljs-comment"># 加上 -fcommon：</span><br>HOSTCFLAGS_DTC := -fcommon -I$(src) -I$(src)/libfdt<br></code></pre></td></tr></table></figure><p><strong>坑 4：lzop 未安装</strong></p><p>内核默认用 LZO 压缩，但系统没装 lzop。要么装包，要么切 gzip：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">scripts/config --<span class="hljs-built_in">disable</span> KERNEL_LZO --<span class="hljs-built_in">enable</span> KERNEL_GZIP<br></code></pre></td></tr></table></figure><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 编译 zImage</span><br>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- zImage -j$(<span class="hljs-built_in">nproc</span>)<br><br><span class="hljs-comment"># 编译设备树（根据屏幕选）</span><br>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- \<br>  imx6ull-14x14-emmc-7-800x480-c.dtb \<br>  imx6ull-14x14-emmc-4.3-800x480-c.dtb \<br>  imx6ull-14x14-emmc-4.3-480x272-c.dtb \<br>  imx6ull-14x14-emmc-hdmi.dtb<br><br><span class="hljs-comment"># 安装到 TFTP 目录</span><br><span class="hljs-built_in">cp</span> <span class="hljs-built_in">arch</span>/arm/boot/zImage ~/linux/tftpboot/<br><span class="hljs-built_in">cp</span> <span class="hljs-built_in">arch</span>/arm/boot/dts/imx6ull-14x14-emmc-*.dtb ~/linux/tftpboot/<br></code></pre></td></tr></table></figure><h2 id="U-Boot-启动配置"><a href="#U-Boot-启动配置" class="headerlink" title="U-Boot 启动配置"></a>U-Boot 启动配置</h2><p>开发板上电，按任意键进 U-Boot 命令行：</p><p><img src="/img/linux_alpha/uboot_start.png" alt="U-Boot 启动界面"></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 网络配置</span><br>setenv serverip 192.168.1.100      <span class="hljs-comment"># PC 的 IP</span><br>setenv ipaddr 192.168.1.200        <span class="hljs-comment"># 开发板的 IP</span><br><br><span class="hljs-comment"># 启动参数（一整行，不要换行！）</span><br>setenv bootargs <span class="hljs-string">&#x27;console=ttymxc0,115200 root=/dev/nfs nfsroot=192.168.1.100:/home/xhd/linux/nfs_rootfs,v3,tcp ip=192.168.1.200::192.168.1.1:255.255.255.0::eth0:off&#x27;</span><br><br><span class="hljs-comment"># 下载并启动</span><br>tftp 80800000 zImage<br>tftp 83000000 imx6ull-14x14-emmc-7-800x480-c.dtb<br>bootz 80800000 - 83000000<br></code></pre></td></tr></table></figure><p><img src="/img/linux_alpha/uboot-tftp.png" alt="TFTP 下载内核和 DTB"></p><p>确认启动正常后，固化：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">setenv bootcmd <span class="hljs-string">&#x27;tftp 80800000 zImage; tftp 83000000 imx6ull-14x14-emmc-7-800x480-c.dtb; bootz 80800000 - 83000000&#x27;</span><br>saveenv<br></code></pre></td></tr></table></figure><h3 id="bootargs-参数详解"><a href="#bootargs-参数详解" class="headerlink" title="bootargs 参数详解"></a>bootargs 参数详解</h3><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">ip</span>=<span class="hljs-number">192.168.1.200</span>::<span class="hljs-number">192.168.1.1:255</span>.<span class="hljs-number">255</span>.<span class="hljs-number">255</span>.<span class="hljs-number">0</span>::eth0:<span class="hljs-literal">off</span><br></code></pre></td></tr></table></figure><p><code>ip=</code> 的 7 个字段（冒号分隔）：</p><figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs ada">板子IP : 服务器<span class="hljs-type">IP</span> : 网关 : 掩码 : 主机名 : 网卡 : 是否<span class="hljs-type">DHCP</span><br>  <span class="hljs-number">200</span>   :   (空)   :  1  : 255.0 : (空)  : <span class="hljs-type">eth0</span> :  <span class="hljs-type">off</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>坑 5</strong>：别用 <code>ip=dhcp</code>！板子和 PC 直连的网段没有 DHCP 服务器，内核会一直卡在 “Sending DHCP requests …”。</p></blockquote><blockquote><p><strong>坑 6</strong>：<code>nfsroot</code> 里加 <code>,v3,tcp</code> 是关键——尽管内核和 NFS 服务器都支持 v4，但 NFS root over v4 在旧内核上问题很多，强制 v3 省心。</p></blockquote><h2 id="启动流程"><a href="#启动流程" class="headerlink" title="启动流程"></a>启动流程</h2><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs elixir">板子上电<br>  │<br>  └→ U-<span class="hljs-title class_">Boot</span> (eMMC)<br>       ├─ tftp <span class="hljs-number">80800000</span> zImage     ← 从 <span class="hljs-title class_">PC</span> 下载内核 (<span class="hljs-number">5.9</span>MB)<br>       ├─ tftp <span class="hljs-number">83000000</span> *.dtb       ← 从 <span class="hljs-title class_">PC</span> 下载设备树 (<span class="hljs-number">40</span>KB)<br>       └─ bootz 启动<br>            │<br>            ├─ <span class="hljs-title class_">FEC</span> 网卡驱动初始化 (<span class="hljs-title class_">CONFIG_FEC</span>=y, 内置)<br>            ├─ 配置静态 <span class="hljs-title class_">IP</span> (<span class="hljs-number">192.168</span>.<span class="hljs-number">1.200</span>)<br>            ├─ <span class="hljs-title class_">NFS</span> mount <span class="hljs-number">192.168</span>.<span class="hljs-number">1.100</span><span class="hljs-symbol">:/home/xhd/linux/nfs_rootfs</span><br>            └─ /sbin/init → 启动完成<br></code></pre></td></tr></table></figure><h2 id="常见问题速查"><a href="#常见问题速查" class="headerlink" title="常见问题速查"></a>常见问题速查</h2><table><thead><tr><th>现象</th><th>原因</th><th>解决</th></tr></thead><tbody><tr><td>U-Boot ping 不通 PC</td><td>PC 有线网口没配 IP 或被 NetworkManager 重置</td><td><code>nmcli</code> 配置静态连接</td></tr><tr><td>TFTP 下载 <code>Abort</code> &#x2F; <code>File not found</code></td><td>tftpd 服务没启动或目录配错</td><td><code>systemctl start tftpd</code>，检查 <code>/etc/conf.d/tftpd</code></td></tr><tr><td>内核卡在 “Sending DHCP requests”</td><td>没有 DHCP 服务器</td><td><code>ip=</code> 改用静态 IP</td></tr><tr><td>NFS mount 失败 “Unable to mount root fs”</td><td>内核缺 <code>CONFIG_ROOT_NFS=y</code> 或 FEC 是模块</td><td>检查 <code>.config</code>，重新编译</td></tr><tr><td>NFS 挂载超时</td><td>NFS 版本不匹配</td><td><code>nfsroot</code> 加 <code>,v3</code></td></tr></tbody></table><h2 id="目录结构整理"><a href="#目录结构整理" class="headerlink" title="目录结构整理"></a>目录结构整理</h2><p>折腾完后的整洁布局：</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs nix">~<span class="hljs-operator">/</span>linux<span class="hljs-symbol">/</span><br>├── kernel<span class="hljs-operator">/</span>linux-imx-<span class="hljs-number">4.1</span>.<span class="hljs-number">15</span><span class="hljs-symbol">/</span>    内核源码 (<span class="hljs-number">841</span>MB, 已 make clean)<br>├── uboot<span class="hljs-operator">/</span>uboot-imx-<span class="hljs-number">2016.03</span><span class="hljs-symbol">/</span>    U-Boot 源码 (<span class="hljs-number">134</span>MB)<br>├── firmware<span class="hljs-operator">/</span>OS_Firmware<span class="hljs-symbol">/</span>        mfgtool 烧写固件 (<span class="hljs-number">250</span>MB)<br>├── tftpboot<span class="hljs-symbol">/</span>                   TFTP 目录 (zImage <span class="hljs-operator">+</span> DTB)<br>├── nfs_rootfs<span class="hljs-symbol">/</span>                 NFS 根文件系统<br>└── vendor<span class="hljs-symbol">/</span>                     正点原子原始资料<br></code></pre></td></tr></table></figure><p><img src="/img/linux_alpha/dir_layout.png" alt="PC 端目录结构"></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>搞通 TFTP + NFS 启动后，开发效率提升巨大：</p><ul><li>改完代码 → <code>make</code> → 板子 <code>reset</code> → 跑起来了，全程在 PC 完成</li><li>不用反复拔插 SD 卡，不用每次都烧写</li><li>板子上跑的文件直接存在 PC 的 <code>nfs_rootfs</code> 里，想看日志、改配置文件直接 PC 上操作</li></ul><p>折腾过程中最核心的三个教训：</p><ol><li><strong>FEC 驱动必须内置（<code>=y</code>）</strong>，不能是模块</li><li><strong>NFS root 用 v3 别用 v4</strong>，旧内核上 v4 坑多</li><li><strong>静态 IP 别用 <code>ip addr add</code></strong>，NetworkManager 会抢回去，用 <code>nmcli</code> 配置持久连接</li></ol><p>希望这篇文章帮你少踩几个坑。欢迎在评论区交流 🚀</p>]]></content>
    
    
    
    <tags>
      
      <tag>嵌入式Linux</tag>
      
      <tag>i.MX6ULL</tag>
      
      <tag>NFS</tag>
      
      <tag>TFTP</tag>
      
      <tag>U-Boot</tag>
      
      <tag>内核编译</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>linux文件系统知识</title>
    <link href="/2026/04/26/linux_fs_knowledge/"/>
    <url>/2026/04/26/linux_fs_knowledge/</url>
    
    <content type="html"><![CDATA[<h1 id="🚀-Linux-文件系统知识大揭秘"><a href="#🚀-Linux-文件系统知识大揭秘" class="headerlink" title="🚀 Linux 文件系统知识大揭秘"></a>🚀 Linux 文件系统知识大揭秘</h1><blockquote><p>本文档整理自与AI的聊天记录，深入探索开发板启动、文件系统、分区等核心知识。</p></blockquote><hr><h2 id="🔥-问题-1-开发板从SD卡启动的奥秘"><a href="#🔥-问题-1-开发板从SD卡启动的奥秘" class="headerlink" title="🔥 问题 1: 开发板从SD卡启动的奥秘"></a>🔥 问题 1: 开发板从SD卡启动的奥秘</h2><p><strong>用户提问：</strong><br>某些开发板可以从SD卡运行操作系统，我想问的是，SD卡里是我们烧录的img文件，开发板有BIOS吗？他是有SD卡的驱动？他默认从SD卡处运行代码？还是自带的SD卡驱动把img从SD卡加载到内存运行？</p><h3 id="💡-AI-回答"><a href="#💡-AI-回答" class="headerlink" title="💡 AI 回答"></a>💡 AI 回答</h3><p><strong>核心结论：</strong><br>ARM开发板<strong>没有PC那种BIOS</strong>；芯片内部ROM固化了一段极小Bootloader；它自带SD卡硬件控制器+只读ROM驱动；开机先初始化SD卡，再把SD卡里的系统镜像读到DDR内存，最后跳转到内存运行Linux。</p><h4 id="🖥️-PC电脑-vs-ARM开发板对比"><a href="#🖥️-PC电脑-vs-ARM开发板对比" class="headerlink" title="🖥️ PC电脑 vs ARM开发板对比"></a>🖥️ PC电脑 vs ARM开发板对比</h4><ul><li><strong>PC电脑</strong>：有BIOS，负责自检、初始化硬盘、读取硬盘系统、加载到内存。</li><li><strong>ARM开发板</strong>：没有BIOS，只有芯片内部Mask ROM（出厂固化只读ROM）。</li></ul><h4 id="⚡-完整开机全过程（一步一步）"><a href="#⚡-完整开机全过程（一步一步）" class="headerlink" title="⚡ 完整开机全过程（一步一步）"></a>⚡ 完整开机全过程（一步一步）</h4><ol><li><strong>上电第一秒</strong>：芯片内部Mask ROM运行，初始化CPU、时钟、自带内置SD卡控制器驱动。默认启动顺序：SD卡 → eMMC → Flash → UART下载。</li><li><strong>ROM固件读取SD卡里的引导程序（U-Boot）</strong>：img镜像结构包括U-Boot、设备树、Linux内核、根文件系统。ROM用自带SD驱动把U-Boot读到DDR内存。</li><li><strong>U-Boot运行</strong>：继续读SD卡，把Linux内核、设备树读到内存。</li><li><strong>U-Boot启动Linux内核</strong>：CPU跳转到内存里的Linux内核。</li><li><strong>Linux内核启动后</strong>：加载Linux版本SD卡驱动，挂载SD卡里的根文件系统。</li></ol><h4 id="❓-具体问题解答"><a href="#❓-具体问题解答" class="headerlink" title="❓ 具体问题解答"></a>❓ 具体问题解答</h4><ul><li><strong>开发板有BIOS吗？</strong> 没有，只有芯片内部Mask ROM固化启动代码。</li><li><strong>开发板有SD卡驱动吗？</strong> 有三层：ROM固化最小SD只读驱动、U-Boot里的SD驱动、Linux内核里的SD驱动。</li><li><strong>默认从SD卡运行代码？</strong> 是，默认优先从SD启动。</li><li><strong>自带SD驱动把img从SD加载到内存运行？</strong> 是，Linux在内存运行，SD卡是仓库。</li></ul><h4 id="🏗️-层级总结"><a href="#🏗️-层级总结" class="headerlink" title="🏗️ 层级总结"></a>🏗️ 层级总结</h4><ol><li><strong>芯片内部Mask ROM</strong>：最底层、固化、自带SD驱动。</li><li><strong>U-Boot引导程序</strong>：第二级、裸机、有SD驱动。</li><li><strong>Linux内核</strong>：操作系统、有SD卡驱动。</li><li><strong>应用层</strong>：用户程序。</li></ol><hr><h2 id="🔥-问题-2-fire-config机制解密"><a href="#🔥-问题-2-fire-config机制解密" class="headerlink" title="🔥 问题 2: fire-config机制解密"></a>🔥 问题 2: fire-config机制解密</h2><p><strong>用户提问：</strong><br>fire-config旨在提供一些常见的系统功能配置服务，在进行配置过程中，这可能会导致&#x2F;boot&#x2F;uEnv.txt或者是其他各种标准的linux配置文件被自动更改了，某些选项需要重启才能生效，如果您修改了其中一个，fire-config 会在<Finish> 按钮被选择时，询问您是否要立即重启，如果您希望配置马上生效，确定重启系统即可。<br>我的理解是fire是用户态进程？还是uboot里的某个程序？不管怎么说，他肯定是修改了某个文件，而这个文件是系统启动的时候会读取的配置文件对吧？</p><h3 id="💡-AI-回答-1"><a href="#💡-AI-回答-1" class="headerlink" title="💡 AI 回答"></a>💡 AI 回答</h3><p>fire-config 是<strong>Linux 用户态进程（应用程序）</strong>，不是U-Boot里的程序。它修改 <code>/boot/uEnv.txt</code> 和其他配置文件，而uEnv.txt是U-Boot启动阶段读取的配置文件。</p><h4 id="🔗-逻辑链条"><a href="#🔗-逻辑链条" class="headerlink" title="🔗 逻辑链条"></a>🔗 逻辑链条</h4><ul><li>Linux运行后，用户执行 <code>sudo fire-config</code>。</li><li>fire-config修改 <code>/boot/uEnv.txt</code>。</li><li>uEnv.txt是U-Boot用的，必须重启让U-Boot重新读取。</li></ul><h4 id="🏗️-三层关系"><a href="#🏗️-三层关系" class="headerlink" title="🏗️ 三层关系"></a>🏗️ 三层关系</h4><ol><li><strong>fire-config</strong>：Linux用户态应用程序，修改SD卡里的 &#x2F;boot&#x2F;uEnv.txt。</li><li><strong>uEnv.txt</strong>：SD卡 &#x2F;boot 分区里的文本配置文件，U-Boot启动时读取。</li><li><strong>U-Boot</strong>：开机读uEnv.txt，按配置加载内核。</li></ol><hr><h2 id="🔥-问题-3-uEnv-txt的位置和SD卡分区探秘"><a href="#🔥-问题-3-uEnv-txt的位置和SD卡分区探秘" class="headerlink" title="🔥 问题 3: uEnv.txt的位置和SD卡分区探秘"></a>🔥 问题 3: uEnv.txt的位置和SD卡分区探秘</h2><p><strong>用户提问：</strong><br>uEnv.txt是在sd卡的哪个位置？sd卡有哪几个分区？sd卡里的内容是不是按照特定格式存储，比如你说的&#x2F;boot分区下的uEnv.txt文件，这个&#x2F;boot分区是什么？这个&#x2F;boot分区是不是uboot可以读取，后面的linux也可以读取？</p><h3 id="💡-AI-回答-2"><a href="#💡-AI-回答-2" class="headerlink" title="💡 AI 回答"></a>💡 AI 回答</h3><p>SD卡分成多个分区，默认2个：BOOT分区（FAT32，存放uEnv.txt等）和RootFS分区（ext4，根文件系统）。</p><ul><li><strong>uEnv.txt位置</strong>：SD卡第一个FAT32分区根目录，Linux路径 <code>/boot/uEnv.txt</code>。</li><li><strong>分区结构</strong>：<ul><li>分区1：FAT32 &#x2F;boot启动分区（U-Boot可读，Linux可读写）。</li><li>分区2：ext4根文件系统分区（U-Boot不能读，Linux专属）。</li></ul></li><li><strong>&#x2F;boot分区</strong>：U-Boot专用FAT32启动分区，存放开机文件。</li><li><strong>读写权限</strong>：U-Boot只能读FAT32的&#x2F;boot，Linux能读写&#x2F;boot和ext4。</li></ul><hr><h2 id="🔥-问题-4-分区和文件系统的深度理解"><a href="#🔥-问题-4-分区和文件系统的深度理解" class="headerlink" title="🔥 问题 4: 分区和文件系统的深度理解"></a>🔥 问题 4: 分区和文件系统的深度理解</h2><p><strong>用户提问：</strong><br>我好像知道了，所谓分区，其实都是逻辑概念，实际上就一个sd卡，这个sd卡上烧录了两个文件系统，一个fat,一个ext4,不同的文件系统里存放了不同的内容，对于软件或者系统来说，他们认识的sd卡只是一个硬件而已，里面的文件系统才是他们想要的理想的格式的数据，rom是怎么从sd卡里搞到uboot的？他不仅有sd卡驱动，也有fat文件系统驱动？不然他怎么从磁盘里搞到uboot呢？在uboot运行的时候，uboot肯定也有fat驱动和sd卡驱动，从而访问fat里的数据，当linux运行之后，linux将fat挂载到&#x2F;boot,自己的跟根文件系统挂载到&#x2F;，所以linux里操作&#x2F;boot下的文件其实就是操作fat下的文件，重启之后uboot就会看到linux改动过后的文件了？</p><h3 id="💡-AI-回答-3"><a href="#💡-AI-回答-3" class="headerlink" title="💡 AI 回答"></a>💡 AI 回答</h3><p>理解完全正确。分区是逻辑划分，SD卡物理一块空间。ROM有SD驱动和极简FAT驱动，能读取FAT分区里的u-boot.bin。U-Boot有SD+FAT驱动，能访问FAT数据。Linux挂载FAT到&#x2F;boot，ext4到&#x2F;，修改&#x2F;boot文件就是修改SD卡FAT分区，重启后U-Boot看到修改。</p><h4 id="🔄-终极闭环逻辑"><a href="#🔄-终极闭环逻辑" class="headerlink" title="🔄 终极闭环逻辑"></a>🔄 终极闭环逻辑</h4><ol><li>SD物理一块空间 → 逻辑分成FAT32 &#x2F;boot、ext4 rootfs。</li><li>ROM：SD+FAT驱动 → 读FAT里的U-Boot → 加载到内存。</li><li>U-Boot：SD+FAT驱动 → 读FAT里uEnv.txt等 → 启动Linux。</li><li>Linux：SD+FAT+ext4驱动 → 挂载FAT到&#x2F;boot，ext4到&#x2F;。</li><li>fire-config修改 &#x2F;boot&#x2F;uEnv.txt → 写入SD卡FAT。</li><li>重启 → U-Boot读取修改后的uEnv.txt。</li></ol><hr><h2 id="🔥-问题-5-目录和dentry的内核级理解"><a href="#🔥-问题-5-目录和dentry的内核级理解" class="headerlink" title="🔥 问题 5: 目录和dentry的内核级理解"></a>🔥 问题 5: 目录和dentry的内核级理解</h2><p><strong>用户提问：</strong><br>目录也是一个特殊文件，他的信息记录在结构体dentry里面，这个结构体描述了这个目录的信息，比如inode指针，是否挂载到了其他文件系统以及驱动，当用户访问&#x2F;boot&#x2F;xxx的时候，访问到&#x2F;会去ext4里找boot，找到了，发现boot的dentry信息里表明这个目录是挂载到sd卡第一个分区fat的，所以就会换一套fat驱动，去第一个分区找文件，是不是？</p><h3 id="💡-AI-回答-4"><a href="#💡-AI-回答-4" class="headerlink" title="💡 AI 回答"></a>💡 AI 回答</h3><p>完全正确。目录是特殊文件，信息在dentry结构体中，包括inode指针、挂载标记等。访问 &#x2F;boot&#x2F;xxx 时，从 &#x2F; 开始在ext4找boot，发现dentry标记挂载到FAT，内核切换文件系统到FAT查找文件。</p><h4 id="🧠-内核逻辑"><a href="#🧠-内核逻辑" class="headerlink" title="🧠 内核逻辑"></a>🧠 内核逻辑</h4><p>访问 &#x2F;boot&#x2F;uEnv.txt：</p><ol><li>从 &#x2F;（ext4）开始。</li><li>查找boot目录。</li><li>查看dentry，发现mnt指针非空（挂载点）。</li><li>切换到FAT32文件系统。</li><li>在FAT32查找uEnv.txt。</li></ol><h4 id="📝-总结"><a href="#📝-总结" class="headerlink" title="📝 总结"></a>📝 总结</h4><p>挂载 &#x3D; 给目录dentry打标记，跳转到另一个文件系统。路径解析 &#x3D; 一层找目录，遇挂载标记切换文件系统。</p><hr><blockquote><p>🎉 恭喜你！通过这些问题，你已经掌握了Linux文件系统的核心奥秘。继续探索吧！ 🚀</p></blockquote>]]></content>
    
    
    
    <tags>
      
      <tag>linux</tag>
      
      <tag>文件系统</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>easy_flash移植</title>
    <link href="/2026/03/31/easy-flash%E7%A7%BB%E6%A4%8D/"/>
    <url>/2026/03/31/easy-flash%E7%A7%BB%E6%A4%8D/</url>
    
    <content type="html"><![CDATA[<h2 id="EasyFlash-移植与踩坑记录"><a href="#EasyFlash-移植与踩坑记录" class="headerlink" title="EasyFlash 移植与踩坑记录"></a>EasyFlash 移植与踩坑记录</h2><h3 id="1-简介"><a href="#1-简介" class="headerlink" title="1. 简介"></a>1. 简介</h3><p>EasyFlash 是一个基于键值（Key-Value）存储的 Flash 管理库，非常适合用于嵌入式设备的标定数据管理。</p><p><strong>我的平台环境：</strong></p><ul><li>32位单片机</li><li>Flash 扇区大小：512B</li><li>Flash 写入粒度：2 word &#x3D; 8 byte</li><li>只使用 env 模式</li></ul><h3 id="2-基本移植步骤"><a href="#2-基本移植步骤" class="headerlink" title="2. 基本移植步骤"></a>2. 基本移植步骤</h3><p>按照官方 README，将相关文件移植到项目中，实现 <code>ef_port.c</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><code class="hljs c">EfErrCode <span class="hljs-title function_">ef_port_read</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> addr, <span class="hljs-type">uint32_t</span> *buf, <span class="hljs-type">size_t</span> size)</span> &#123;<br>    <span class="hljs-comment">//简单</span><br>&#125;<br><br>EfErrCode <span class="hljs-title function_">ef_port_erase</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> addr, <span class="hljs-type">size_t</span> size)</span> &#123;<br>    <span class="hljs-comment">//简单</span><br>&#125;<br><br>EfErrCode <span class="hljs-title function_">ef_port_write</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> addr, <span class="hljs-type">const</span> <span class="hljs-type">uint32_t</span> *buf, <span class="hljs-type">size_t</span> size)</span> &#123;<br>    EfErrCode result = EF_NO_ERR;<br>    <span class="hljs-type">size_t</span> i;<br>    <span class="hljs-type">uint64_t</span> write_data, read_data;<br>    EF_ASSERT(addr % <span class="hljs-number">8</span> == <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; size; i += <span class="hljs-number">8</span>) &#123;<br>        <span class="hljs-comment">//由于目标单片机一次写入两个字（2 word），`ef_port_write` 需要注意字节序：</span><br>        write_data = ((<span class="hljs-type">uint64_t</span>)buf[<span class="hljs-number">1</span>] &lt;&lt; <span class="hljs-number">32</span>) | buf[<span class="hljs-number">0</span>];<br>        FLASH_Write2WordsWithECC(addr, write_data);<br>        read_data = *(<span class="hljs-type">uint64_t</span> *)addr;<br>        <span class="hljs-keyword">if</span> (read_data != write_data) &#123;<br>            result = EF_WRITE_ERR;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        buf += <span class="hljs-number">2</span>;<br>        addr += <span class="hljs-number">8</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br><br><span class="hljs-type">void</span> <span class="hljs-title function_">ef_log_debug</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *file, <span class="hljs-type">const</span> <span class="hljs-type">long</span> line, <span class="hljs-type">const</span> <span class="hljs-type">char</span> *format, ...)</span> &#123;<br><span class="hljs-meta">#<span class="hljs-keyword">ifdef</span> PRINT_DEBUG</span><br>    va_list args;<br>    va_start(args, format);<br>    <span class="hljs-built_in">vprintf</span>(format, args);<br>    va_end(args);<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;<br><br><span class="hljs-type">void</span> <span class="hljs-title function_">ef_log_info</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *format, ...)</span> &#123;<br><span class="hljs-meta">#<span class="hljs-keyword">ifdef</span> PRINT_DEBUG</span><br>    va_list args;<br>    va_start(args, format);<br>    <span class="hljs-built_in">vprintf</span>(format, args);<br>    va_end(args);<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;<br><br><span class="hljs-type">void</span> <span class="hljs-title function_">ef_print</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *format, ...)</span> &#123;<br><span class="hljs-meta">#<span class="hljs-keyword">ifdef</span> PRINT_DEBUG</span><br>    va_list args;<br>    va_start(args, format);<br>    <span class="hljs-built_in">vprintf</span>(format, args);<br>    va_end(args);<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;<br></code></pre></td></tr></table></figure><p>打开 IAR 半主机模式，直接使用打印函数vprintf(format, args);通过 JLink 的 SWD 接口与 PC 通信。<br><img src="/img/easyflash/iar_semihosted.png" alt="IAR半主机模式"></p><h3 id="3-问题一：env-丢失"><a href="#3-问题一：env-丢失" class="headerlink" title="3. 问题一：env 丢失"></a>3. 问题一：env 丢失</h3><p>接口移植完成后，用 demo 测试，<code>boot_times</code> 每次重启都能读取并自增。但发现：一个扇区只能存放 5 条 <code>boot_times</code> 记录，第 6 次写入前，理论上应读取第 5 条记录并自增写入<code>boot_times = 6</code>到第二个扇区，但实际写到第二个扇区的 <code>boot_times = 1</code>。</p><p><img src="/img/easyflash/ef_env_lost.png" alt="env丢失"></p><h4 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h4><p>调试发现，第五次写入 flash 没问题，问题出在下一次重启后，<code>ef_get_env</code> 读取第一个扇区时，前 4 条记录都是无效的（已删除），准备读取下一条记录时（<code>ef_env.c -&gt; find_next_env_addr()</code>），返回地址失败。</p><p><img src="/img/easyflash/ef_find_next.png" alt="find_next_env_addr问题"></p><p>原因：</p><ul><li>该函数在给定地址范围内寻找 env magic，但参数传递时 <code>end = sector-&gt;addr + SECTOR_SIZE - SECTOR_HDR_DATA_SIZE</code>，而 magic 恰好在 end 之后，导致没找到有效记录。</li><li>按理说扇区元数据应在扇区头（起始地址），不应在这里减去 <code>SECTOR_HDR_DATA_SIZE</code>。</li></ul><p><strong>修正：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// xhd add</span><br>addr = find_next_env_addr(addr, sector-&gt;addr + SECTOR_SIZE - <span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><p><img src="/img/easyflash/ef_boot_times_ok.png" alt="运行正常"></p><p>问题解决，第二个扇区正确写入 <code>boot_times = 6</code>。</p><h3 id="问题二：垃圾回收-Hard-Fault"><a href="#问题二：垃圾回收-Hard-Fault" class="headerlink" title="问题二：垃圾回收 Hard Fault"></a>问题二：垃圾回收 Hard Fault</h3><p>给 EasyFlash 分配 4 个扇区，起始地址0xf800, <code>EF_GC_EMPTY_SEC_THRESHOLD = 1</code>。不断更改 env 时，一直追加到第 3 个扇区快满，触发垃圾回收，程序崩溃。</p><p>垃圾回收会将有效数据搬运到最后一个空扇区，依赖 <code>move_env</code> 实现。调试发现第一次 <code>move_env</code> 成功，第二次崩溃。</p><p>调用链：<code>alloc_env -&gt; sector_iterator -&gt; traversal_env = true -&gt; read_sector_meta_data -&gt; get_next_env_addr -&gt; find_next_env_addr</code></p><p>又回到了 <code>find_next_env_addr</code>，此时参数 <code>start = pre_env-&gt;addr.start + pre_env-&gt;len</code>，<code>end = 本扇区结束地址</code>。该函数内部读取数据时，访问了 end 之后的地址，而此扇区正好是 flash 最后一个扇区，直接导致 hard fault。</p><p><img src="/img/easyflash/ef_mem_out.png" alt="内存越界"></p><p><strong>修正：保证读取不越界</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-type">uint32_t</span> <span class="hljs-title function_">find_next_env_addr</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> start, <span class="hljs-type">uint32_t</span> end)</span> &#123;<br>    <span class="hljs-type">uint8_t</span> buf[<span class="hljs-number">32</span>];<br>    <span class="hljs-type">uint32_t</span> start_bak = start, i;<br>    <span class="hljs-type">uint32_t</span> magic;<br><br><span class="hljs-meta">#<span class="hljs-keyword">ifdef</span> EF_ENV_USING_CACHE</span><br>    <span class="hljs-type">uint32_t</span> empty_env;<br>    <span class="hljs-keyword">if</span> (get_sector_from_cache(EF_ALIGN_DOWN(start, SECTOR_SIZE), &amp;empty_env)<br>            &amp;&amp; start == empty_env) &#123;<br>        <span class="hljs-keyword">return</span> FAILED_ADDR;<br>    &#125;<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br><br>    <span class="hljs-keyword">if</span> (end &lt; <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>) || start &gt;= end - <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>) + <span class="hljs-number">1</span>) &#123;<br>        <span class="hljs-keyword">return</span> FAILED_ADDR;<br>    &#125;<br><br>    <span class="hljs-keyword">for</span> (; start &lt; end; start += (<span class="hljs-keyword">sizeof</span>(buf) - <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>))) &#123;<br>        <span class="hljs-type">uint32_t</span> readable = end - start;<br>        <span class="hljs-type">uint32_t</span> read_len = <span class="hljs-keyword">sizeof</span>(buf);<br>        <span class="hljs-keyword">if</span> (readable &lt; read_len) read_len = readable;<br>        <span class="hljs-type">uint32_t</span> aligned_len = (read_len + <span class="hljs-number">3u</span>) &amp; ~<span class="hljs-number">3u</span>;<br>        <span class="hljs-keyword">if</span> (aligned_len &gt; <span class="hljs-keyword">sizeof</span>(buf)) aligned_len = <span class="hljs-keyword">sizeof</span>(buf);<br>        ef_port_read(start, (<span class="hljs-type">uint32_t</span> *) buf, aligned_len);<br><br>        <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-keyword">sizeof</span>(buf) - <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>); i++) &#123;<br>            <span class="hljs-keyword">if</span> (start + i + <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>) &gt; end) <span class="hljs-keyword">break</span>;<br>            <span class="hljs-keyword">if</span> (i + <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">uint32_t</span>) &gt; aligned_len) <span class="hljs-keyword">break</span>;<br><span class="hljs-meta">#<span class="hljs-keyword">ifndef</span> EF_BIG_ENDIAN</span><br>            magic = buf[i]<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i + <span class="hljs-number">1</span>] &lt;&lt; <span class="hljs-number">8</span>)<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i + <span class="hljs-number">2</span>] &lt;&lt; <span class="hljs-number">16</span>)<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i + <span class="hljs-number">3</span>] &lt;&lt; <span class="hljs-number">24</span>);<br><span class="hljs-meta">#<span class="hljs-keyword">else</span></span><br>            magic = buf[i + <span class="hljs-number">3</span>]<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i + <span class="hljs-number">2</span>] &lt;&lt; <span class="hljs-number">8</span>)<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i + <span class="hljs-number">1</span>] &lt;&lt; <span class="hljs-number">16</span>)<br>                  + ((<span class="hljs-type">uint32_t</span>)buf[i]     &lt;&lt; <span class="hljs-number">24</span>);<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>            <span class="hljs-keyword">if</span> (magic == ENV_MAGIC_WORD &amp;&amp; (start + i - ENV_MAGIC_OFFSET) &gt;= start_bak) &#123;<br>                <span class="hljs-keyword">return</span> start + i - ENV_MAGIC_OFFSET;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> FAILED_ADDR;<br>&#125;<br></code></pre></td></tr></table></figure><p>修正后垃圾回收正常。</p><h3 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h3><table><thead><tr><th>问题</th><th>原因</th><th>修正</th></tr></thead><tbody><tr><td>env 丢失，boot_times 重置为 1</td><td><code>find_next_env_addr</code> 的 end 参数偏小，漏掉最后一条有效记录</td><td>end 不减 <code>SECTOR_HDR_DATA_SIZE</code></td></tr><tr><td>垃圾回收 Hard Fault</td><td><code>find_next_env_addr</code> 读取越界，访问了 flash 末尾之后的地址</td><td>读取前检查边界，限制读取长度</td></tr></tbody></table>]]></content>
    
    
    
    <tags>
      
      <tag>embedded</tag>
      
      <tag>debug</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2026/02/17/hello-world/"/>
    <url>/2026/02/17/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
